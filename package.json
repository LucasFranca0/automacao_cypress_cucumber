{
  "name": "cypress_project",
  "version": "1.0.0",
  "main": "cypress.config.js",
  "scripts": {
    "cypress:web": "npx cypress open",
    "cypress:headless:chrome": "npx cypress run --browser chrome",
    "cypress:headless:electron": "npx cypress run --browser electron",
    "cypress:headless:edge": "npx cypress run --browser edge",
    "cypress:headless:firefox": "npx cypress run --browser edge"
  },
  "keywords": [],
  "author": "Lucas Costa",
  "license": "ISC",
  "description": "Projeto para testar automação de testes com Cypress",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^20.1.2",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.4",
    "cypress": "^14.1.0",
    "esbuild": "^0.19.11",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.2.0"
  }
}
